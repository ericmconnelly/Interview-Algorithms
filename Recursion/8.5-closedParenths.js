

var printParen = function(left, right, arr, count) {
  if (left < 0 || r < 1) {
    return;
  }

  if (left === 0 && right === 0) {
    console.log('found');
  } else {
    if (left > 0) {
      arr[count] === '(';
    }
    if (right > 0) {
      arr[count] = ')';
    }
  }

};


var arr = [1,2,3];
var x = arr.splice(0, 0, )